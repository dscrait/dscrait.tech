import styled from 'styled-components';

const MediumBlogContainer = styled.div`
	position: relative;
	top: 
	left: 0;
	bottom: 0;
	right: 0;
	margin: 0;
	overflow: hidden;
`;

export default MediumBlogContainer;
